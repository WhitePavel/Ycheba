find /home -name "*.txt" - ищет файл по любым переменным
wc - выводит данные(строки,слова,символы)
sort - сортирует файл не изменяя
cut -d "делитель" -отсортирует строчку по делитель

grep -i **** где - ищет нужный текст в файлах
sort names.txt > names_sorted.txt - заменить все
>> - добавить в конец

sort names.txt >names.txts - данная команда стирает файл а потом сортирует)
grep denis /etc/* 2>erros.txt
grep denis /etc/* 2>/dev/null -отправить вникуда
grep denis /etc/* > good.txt 2>erros.txt сохранить первый и второй поток
grep denis /etc/* &>/dev/null - сохранить поток вместе

netstat - проверка портов
sudo ufw allow *** - откртытие порта 
ssh **** -присоединение к серверу 

ss - команда проверки портов
top - показывает процессоры
free -h - показывает память
ps - процессы которые бегут сейчас
ps -aux - показать абсолютно все процессы от всех пользователей
ps -aux | grep ***8
